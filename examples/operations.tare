func main: {
  // with keywords
  add(1, 2);
  sub(1, 2);
  mul(1, 2);
  div(1, 2);
  shl(1, 2);
  shr(1, 2);
  less(1, 2);
  lequal(1, 2);
  greater(1, 2);
  gequal(1, 2);
  equal(1, 2);
  nequal(1, 2);
  
  bitand(1, 2);
  bitor(1, 2);
  logand(1, 2);
  logor(1, 2);
  

  not(tape);
  not(add(head, 3));
  not(head + 3);
  head + 3 * head + 1;
  
  not(1);

  // with special tokens
  1 + 2;
  1 - 2;
  1 * 2;
  1 / 2;
  1 << 2;
  1 >> 2;
  1 <= 2;
  1 >= 2;
  1 < 2;
  1 > 2;
  1 == 2;
  1 != 2;
  1 & 2;
  1 | 2;
  1 && 2;
  1 || 2;
  
  !1;
  
  a(10);
  n();
  a(49);
  n();
  a(32);
  n();
  a(59);
  n();
  a(10);
  n();
  write(base, index);
  ret;
}